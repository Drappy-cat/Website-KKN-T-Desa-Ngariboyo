import { Send } from "lucide-react";

const inputClassName = "w-full px-4 py-3 bg-background border border-border rounded-[16px] text-sm font-body outline-none focus:border-primary transition-colors";
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <div><label className="block text-xs font-semibold text-foreground/60 mb-1.5 uppercase tracking-wider font-caption">{label}</label>{children}</div>; }

export default function ContactForm() { return <div className="bg-white rounded-[24px] p-8 shadow-[0_20px_60px_rgba(0,0,0,0.1)] border border-border"><h3 className="font-display font-bold text-primary text-xl mb-6">Kirim Pesan</h3><form className="space-y-4" onSubmit={(event) => { event.preventDefault(); alert("Pesan terkirim! Kami akan merespons segera."); }}><div className="grid sm:grid-cols-2 gap-4"><Field label="Nama"><input type="text" required placeholder="Nama Anda" className={inputClassName} /></Field><Field label="Email"><input type="email" required placeholder="email@anda.com" className={inputClassName} /></Field></div><Field label="Subjek"><input type="text" required placeholder="Subjek pesan" className={inputClassName} /></Field><Field label="Pesan"><textarea required rows={5} placeholder="Tulis pesan Anda di sini..." className={`${inputClassName} resize-none`} /></Field><button type="submit" className="w-full flex items-center justify-center gap-2 py-3.5 bg-primary text-white font-semibold rounded-[16px] hover:bg-secondary transition-all hover:scale-101 active:scale-99 shadow-md font-body">Kirim Pesan <Send className="w-4 h-4" /></button></form></div>; }